---
title: SRS — Software Requirements
description: The full Software Requirements Specification for App Preview, embedded inline (Docs / Flow / Board views, diagrams).
template: splash
tableOfContents: false
sidebar:
  order: 8
  label: SRS (Requirements)
---

{/* The SRS is a self-contained report at the umbrella root (srs-index.html),
    generated by resources/srs.sh. Embed it inline so it opens in-page (no new
    tab). BASE_URL is `<umbrella>/docs/`; strip the trailing `docs/` to reach the
    umbrella root — works on GitHub Pages, the local build, and every locale. */}
export const SRS_SRC = import.meta.env.BASE_URL.replace(/docs\/?$/, '') + 'srs-index.html';

<iframe
  src={SRS_SRC}
  title="Software Requirements Specification"
  loading="lazy"
  style={{ display: 'block', width: '100%', height: 'calc(100vh - 4rem)', border: 0 }}
/>
